name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      # For mollyferguson.info
      MOLLY_ASTRO_HYGRAPH_ENDPOINT: https://us-east-1-shared-usea1-02.cdn.hygraph.com/content/cm048oo3l01gz07wf7rrq84lg/master
      # For micah-ferguson  
      MICAH_ASTRO_HYGRAPH_ENDPOINT: https://us-west-2.cdn.hygraph.com/content/cm4ah46tx01ol07lnfafy43gg/master
      # GitHub Packages token
      NPM_GH_TOKEN: ${{ secrets.NPM_GH_TOKEN }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run linter
      run: npm run lint

    - name: Run Astro check
      run: npm run astro:check

    - name: Build all projects
      run: npm run build

  # deploy-molly:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   environment: production
    
  #   env:
  #     ASTRO_HYGRAPH_ENDPOINT: https://us-east-1-shared-usea1-02.cdn.hygraph.com/content/cm048oo3l01gz07wf7rrq84lg/master
    
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version-file: '.nvmrc'
  #       cache: 'npm'

  #   - name: Install dependencies
  #     run: npm ci

  #   - name: Build mollyferguson.info
  #     run: npm run build:molly

  #   - name: Deploy to Cloudflare Pages (mollyferguson.info)
  #     uses: cloudflare/pages-action@v1
  #     with:
  #       apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #       accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #       projectName: mollyferguson-info
  #       directory: apps/mollyferguson.info/dist

  # deploy-micah:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   environment: production
    
  #   env:
  #     ASTRO_HYGRAPH_ENDPOINT: https://us-west-2.cdn.hygraph.com/content/cm4ah46tx01ol07lnfafy43gg/master
    
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version-file: '.nvmrc'
  #       cache: 'npm'

  #   - name: Install dependencies
  #     run: npm ci

  #   - name: Build micah-ferguson
  #     run: npm run build:micah

  #   - name: Deploy to Cloudflare Pages (micah-ferguson)
  #     uses: cloudflare/pages-action@v1
  #     with:
  #       apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  #       accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #       projectName: micah-ferguson
  #       directory: apps/micah-ferguson/dist

  publish-utils:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
        registry-url: 'https://npm.pkg.github.com'

    - name: Install dependencies
      run: npm ci

    - name: Build astro-utils
      run: npm run build:utils

    - name: Publish to GitHub Packages
      run: npm publish --workspace=packages/astro-utils
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_GH_TOKEN }} 